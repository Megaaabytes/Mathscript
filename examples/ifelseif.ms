function(main)
	if(9-9=1)
		out(This won't ever execute since 9-9 will never equal 1)
	elseif(9-9=0)
		out(This executed because it matched the second condition of the if statement!)
		newline()
	else()
		out(This executed because this was not what the original statement wanted.)
		newline()
	endif()
pause()