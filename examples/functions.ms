function(foo)
    out("foo")
    newline()
    run(bar)
endf()

function(bar)
    out("bar")
    newline()
    run(test)
    newline()
endf()

function(test)
    out("test")
    newline()
endf()

function(main)
    run(foo)
    pause()
stop(0)