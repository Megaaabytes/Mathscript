function(main)
    def(string,"test",x)
    if(${x}+?=test1)
        out("Hello World!")
        newline()
    elseif(${x}+?=test)
        out("Elseif ran!")
        newline()
    else()
        out("Else ran!")
        newline()
    endif()
pause()