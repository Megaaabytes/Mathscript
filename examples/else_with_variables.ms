 function(main)
    def(string,"test12",x)
    if(${x}+?=test)
        out("Hello World!")
        newline()
    elseif(${x}+?=test1)
        out("Else if!")
        newline()
    else()
        out("Else ran!")
        newline()
    endif()
 pause()