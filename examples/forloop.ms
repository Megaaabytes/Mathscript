function(main)
	def(number,0,arraySize)
	def(string#,10000,array)
	def(number,0,index)
	insert(array,hello)
	insert(array,hello1)
	length(array,arraySize)
	decrement(arraySize)	

	loopuntil(${arraySize})
		infoAt(array#${index})
		newline()
		increment(index)
	endl()
pause()