function(main)
	def(string#,10,array)
	def(number,0,size)

	insert(array,helloworld)
	insert(array,test)
	insert(array,thisismathscript!)

	length(array,size)
	info(size)
	newline();
	pause()